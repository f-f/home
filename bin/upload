#!/bin/bash

if [ -z "$1" ]
  then
    echo "No argument supplied"
    exit 1
fi

FILE=$1

if [ -f $FILE ];
  then
    scp $FILE augustus:///srv/uploads/files && printf "\nFile URL: https://u.ferrai.io/$FILE\n"
else
    echo "File $FILE does not exist."
    exit 1
fi

