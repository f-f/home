#!/bin/bash

if [ -z "$1" ]
  then
    echo "Please supply a filepath"
    exit 1
fi

FILE=$1

if [ -f $FILE ];
then
  ffsend upload --downloads 1 --password --archive --copy --open "${FILE}"
else
  echo "File $FILE does not exist."
  exit 1
fi

